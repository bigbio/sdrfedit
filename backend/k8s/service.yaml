apiVersion: v1
kind: Service
metadata:
  name: sdrf-assistant-service
  namespace: sdrf-assistant
  labels:
    app: sdrf-assistant
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: sdrf-assistant
